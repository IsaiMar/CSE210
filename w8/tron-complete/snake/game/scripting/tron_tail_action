from game.casting.snake import Snake
from action import Action
import time
timeLoop = True
Sec = 0

def TronTrailAction(Action):
    
    def execute(self, cast, script):
        Sec = 0
        while timeLoop:
            Sec += 1
            time.sleep(1)